/**
 * Logs in a user using Supabase authentication
 * @param {string} email - User email address
 * @param {string} password - User password
 * @returns {Promise<boolean>} Returns true if login is successful, otherwise false
 * @throws {Error} If login fails due to invalid credentials or network issues
 * @example
 * const success = await loginUser("test@email.com", "123456");
 */
async function loginUser(email, password) {
  // logic
}